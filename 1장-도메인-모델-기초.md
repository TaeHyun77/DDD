### **도메인과 하위 도메인**

---

도메인은 하나의 큰 영역으로 구성되지만, 내부적으로는 여러 **하위 도메인** 으로 나눌 수 있습니다.

하나의 하위 도메인만으로 완전한 기능을 제공하기보다는, 여러 하위 도메인이 서로 연동하여 하나의 완전한 비즈니스 기능을 제공합니다.

또한, 특정 도메인을 위한 소프트웨어라고 해서 해당 도메인이 제공해야 하는 모든 기능을 직접 구현해야 하는 것은 아닙니다. 

ex ) 쇼핑몰에서 배송이나 결제 기능을 직접 개발하지 않고 외부 배송 시스템이나 결제 시스템을 연동하여 사용 가능

따라서, 모든 도메인에 동일한 하위 도메인이 존재하는 것은 아니며, 하위 도메인을 어떻게 구성하고 어디까지 시스템으로 구현할지는 비즈니스 상황에 따라 달라집니다.

ex )  고객에게 혜택을 제공하는 온라인 쇼핑몰이 있다고 할 때, 규모가 큰 쇼핑몰이라면 고객 혜택을 관리하는 별도의 시스템을 구축할 수 있지만, 규모가 작은 쇼핑몰에서는 엑셀과 같은 수작업으로 관리할 수도 있음

즉, 하위 도메인의 구성과 구현 방식은 해당 조직의 비즈니스 상황과 요구사항에 따라 결정됩니다.

### **도메인 전문가와 개발자**

---

각 하위 도메인에는 해당 업무를 잘 알고 있는 도메인 전문가가 존재합니다.

예를 들어 정산 업무를 담당하는 회계 담당자는 기존에 엑셀로 처리하던 업무를 자동화해 달라는 요구사항을 제시할 수 있으며,  AS 기사는 고객에게 보내는 문자 메시지를 빠르게 작성할 수 있도록 자주 사용하는 문구를 템플릿으로 추천해 달라는 요구사항을 제시할 수도 있습니다.

개발자는 이러한 요구사항을 분석하고, 소프트웨어로 구현한 뒤 테스트와 배포까지 수행합니다.

따라서 개발자가 요구사항을 정확하게 이해하는 것이 매우 중요하며, 요구사항을 잘못 이해하면 구현한 기능을 다시 수정해야 하기 때문에 불필요한 작업이 발생하기 때문입니다.

이를 방지하기 위해서는 개발자가 도메인 전문가와 직접 소통하는 것이 중요하며, 요구사항을 여러 사람이 전달하는 과정에서 정보가 왜곡되거나 중요한 내용이 누락될 수 있기 때문입니다.

물론 개발자가 도메인 전문가만큼의 전문 지식을 가질 필요는 없지만, 개발자와 이해관계자 역시 해당 도메인에 대한 기본적인 지식을 갖추어야 합니다.

결국 도메인 전문가, 이해관계자, 개발자가 동일한 도메인 지식을 공유하고 직접 소통할수록 도메인 전문가가 원하는 제품을 정확하게 만들 가능성이 높아집니다.

> 이를 소프트웨어 개발 관점에서 Garbage In, Garbage Out(GIGO) - 잘못된 요구사항이나 정보가 입력되면, 그에 기반한 소프트웨어 역시 잘못된 결과를 만들어낼 가능성이 높다.
> 

### **도메인 모델**

---

도메인 모델에는 다양한 정의와 표현 방식이 존재합니다.

기본적으로 도메인 모델은 특정 도메인을 개념적으로 표현한 모델입니다.

!image.png

예를 들어 주문 도메인을 생각해보면 상품의 개수, 배송지, 상품 가격에 따른 결제 금액, 결제 상태, 배송 상태, 주문 취소 등 여러 개념과 규칙이 서로 연결되어 있습니다.

이러한 개념과 관계를 객체를 이용해 표현할 수도 있습니다.

예를 들어 객체 기반의 도메인 모델을 사용하면 `Order` 객체에 주문번호와 결제해야 할 총 금액이 존재하고, 배송 정보가 변경될 수 있다는 사실 등을 표현할 수 있다.

하지만, 도메인 모델을 반드시 객체로만 표현해야 하는 것은 아닙니다.

!image.png

상태 변화가 중요한 도메인이라면 상태 다이어그램으로 표현할 수 있고, 개념 간의 관계가 중요하다면 그래프를 사용할 수도 있습니다. 또한 복잡한 계산 규칙이 중요한 도메인이라면 수학 공식이나 함수로 표현할 수도 있습니다.

즉, 도메인을 이해하고 도메인 지식을 공유하는 데 도움이 된다면 어떤 표현 방식을 사용하는지는 중요하지 않습니다.

### **개념 모델과 구현 모델**

---

도메인 모델은 기본적으로 도메인 자체를 이해하기 위한 개념적인 모델입니다.

따라서, 도메인 모델을 작성했다고 해서 곧바로 코드를 작성할 수 있는 것은 아니며, 실제 소프트웨어를 개발하기 위해서는 사용하는 프로그래밍 언어나 프레임워크 등의 구현 기술에 맞는 구현 모델이 필요합니다.

개념 모델과 구현 모델은 서로 다른 모델이지만, 구현 모델이 개념 모델의 구조와 의미를 최대한 따르도록 설계할 수 있습니다.

예를 들어 객체 기반으로 도메인을 모델링했다면 객체지향 언어를 사용하여 개념 모델과 유사한 구조로 구현할 수 있으며, 반대로 수학적인 모델을 사용했다면 함수나 수식을 활용하여 해당 모델의 규칙을 구현할 수 있습니다.

결국 도메인 모델의 핵심 목적은 코드를 바로 작성하는 것이 아니라, 도메인을 올바르게 이해하고 도메인 전문가와 개발자가 동일한 지식을 공유하는 것이며, 이후 이 개념을 실제 구현 기술에 맞는 구현 모델로 옮겨 소프트웨어로 구현하게 됩니다.

**하위 도메인과 모델**

도메인은 다수의 하위 도메인으로 구성되며, 각 하위 도메인이 다루는 영역은 서로 다르기에 같은 용어라도 하위 도메인마다 의미가 달라질 수 있습니다.

즉, 도메인에 따라 용어 의미가 결정되므로, 여러 하위 도메인을 하나의 다이어그램에 모델링하면 안됨 → 제대로 이해하는데 방해가 되기 때문이며, 각 하위 도메인마다 별도로 모델을 만들어야 합니다.

### 도메일 모델 표현

---

**아키텍처 구성**

!image.png

- UI 또는 표현 : 사용자의 요청을 받아 처리하고, 그 결과를 사용자에게 보여줍니다. 여기서 사용자는 사람뿐만 아니라 다른 외부 시스템일 수도 있습니다.
- 응용 : 사용자가 요청한 기능을 실행하며, 직접적인 업무 규칙을 구현하기보다는 도메인 계층의 기능을 조합하여 하나의 유스케이스를 수행합니다.
- 도메인 : 시스템의 핵심인 도메인 규칙과 비즈니스 로직을 구현합니다.
- 인프라스트럭처 : 데이터베이스, 메시징 시스템, 외부 API 등 외부 시스템과의 연동을 담당합니다.

도메인 계층은 도메인의 핵심 규칙과 비즈니스 로직을 구현합니다.

예를 들어 주문 도메인에서는 아래와 같은 규칙이 존재할 수 있습니다.

- 출고 전에만 배송지를 변경할 수 있습니다.
- 배송이 시작되기 전에만 주문을 취소할 수 있습니다.

이러한 도메인 규칙을 코드로 구현한 로직이 도메인 계층에 위치하게 됩니다.

그리고 도메인의 핵심 규칙을 객체의 상태와 행위로 표현하여 객체지향적으로 구현하는 방식을 도메인 모델 패턴이라고 합니다.

```kotlin
코드로 표현하면 Order 클래스와 주문 상태를 나타내는 OrderState enum이 존재하고, OrderState가 현재 상태에서 
가능한 행위를 판단하는 메서드를 제공할 수 있습니다. 이를 통해 주문 상태에 따른 변경 가능 여부를 도메인 규칙으로 관리합니다.

다만, 배송지 변경처럼 판단에 주문 자체의 정보가 필요한 경우에는 Order가 해당 규칙을 직접 구현해야 합니다.

중요한 것은 주문과 관련된 핵심 규칙을 Order와 OrderState 같은 도메인 모델 내부에 위치시키는 것입니다. 
이렇게 하면 비즈니스 규칙이 여러 서비스나 외부 코드에 흩어지지 않고, 규칙이 변경되거나 확장될 때 도메인 모델 중심으로 
수정할 수 있어 다른 코드에 미치는 영향을 줄일 수 있습니다.
```

### 도메인 모델 도출

---

도메인을 모델링할 때 가장 기본이 되는 작업은 요구사항에서 도메인 모델의 `핵심 구성요소`, `규칙`, `기능`을 찾아내는 것입니다.

이 과정은 요구사항에서 출발합니다. 예를 들어 주문 도메인에 다음과 같은 요구사항이 있다면, 이를 바탕으로 `Order`에 필요한 기능을 도출할 수 있습니다.

아직 상세한 구현 방법을 결정할 필요는 없으며, 우선 요구사항에 대응하는 기능을 메서드로 표현할 수 있습니다.

```kotlin
public class Order {
	public void 요구사항1() {...}
	public void 요구사항2() {...}
	...
}
```

**주문 항목 도출**

아래와 같은 요구사항이 있다고 했을 때, 이러한 요구사항을 통해 `OrderLine`이라는 모델을 도출할 수 있습니다.

- 주문 항목에는 상품, 가격, 구매 수량이 필요합니다.
- 주문 가격은 `상품 가격 × 수량`으로 계산합니다.
- 주문에는 하나 이상의 상품을 포함해야 합니다.

```kotlin
public class OrderLine {
    private Product product;
    private int price;
    private int quantity;

    public int calculateAmount() {
        return price * quantity;
    }
}
```

그리고 하나 이상의 상품을 주문할 수 있으므로 `Order`는 여러 개의 `OrderLine`을 가집니다. 또한 최소 하나 이상의 주문 항목이 존재해야 한다는 규칙을 `Order`에서 강제할 수 있습니다.

```kotlin
public class Order {
    private List<OrderLine> orderLines;
    private Money totalAmount;

    public Order(List<OrderLine> orderLines) {
        verifyOrderLines(orderLines);
        this.orderLines = orderLines;
        this.totalAmount = calculateTotalAmount();
    }

    private void verifyOrderLines(List<OrderLine> orderLines) {
        if (orderLines == null || orderLines.isEmpty()) {
            throw new IllegalArgumentException();
        }
    }

    private Money calculateTotalAmount() {
        // OrderLine의 금액을 합산
    }
}
```

**배송지 정보 도출**

배송지에 이름, 전화번호, 주소 등의 정보가 필요하다는 요구사항이 있다면 이를 하나의 개념으로 묶어 `ShippingInfo`라는 도메인 모델을 도출할 수 있습니다.

```kotlin
public class ShippingInfo {
    private String name;
    private String phone;
    private Address address;

    // 생성자, getter 등
}
```

또한 주문을 생성할 때 배송지 정보를 반드시 지정해야 한다는 요구사항이 있다면, `Order` 생성자에 `ShippingInfo`를 전달하도록 모델링해야합니다.

```kotlin
public class Order {
    private List<OrderLine> orderLines;
    private ShippingInfo shippingInfo;
    private Money totalAmount;

    public Order(
        List<OrderLine> orderLines,
        ShippingInfo shippingInfo
    ) {
        verifyOrderLines(orderLines);
        verifyShippingInfo(shippingInfo);

        this.orderLines = orderLines;
        this.shippingInfo = shippingInfo;
        this.totalAmount = calculateTotalAmount();
    }

    private void verifyShippingInfo(ShippingInfo shippingInfo) {
        if (shippingInfo == null) {
            throw new IllegalArgumentException();
        }
    }
}
```

**상태 도메인 도출**

도메인 구현 시, 특정 조건이나 상태에 따라 제약이나 규칙이 다르게 적용되는 경우가 존재하는데, 아래와 같습니다.

- 출고하면 배송 정보 변경 불가
- 출고 전에 주문 취소 가능

→ 출고 상태에 따른 제약 사항을 다루고 있으며, 이를 위해 주문은 출고 상태를 표현할 수 있어야 함

또한, `결제 완료 전에는 상품 준비 안한다`는 요구사항도 상태와 관련이 있음

이러한 상태는 열겨 타입으로 표현 가능합니다.

```kotlin
public enum OrderState {
    PAYMENT_WAITING,       // 결제 대기
    PREPARING,             // 상품 준비
    SHIPPED,               // 출고
    DELIVERING,             // 배송 중
    DELIVERY_COMPLETED,    // 배송 완료
    CANCELED               // 취소
}
```

이러한 규칙 적용을 위해 Order에 관련된 요구사항을 구현

```kotlin
public class Order {
    private OrderState orderState;
    private ShippingInfo shippingInfo;

    public Order(OrderState orderState, ShippingInfo shippingInfo) {
        this.orderState = orderState;
        this.shippingInfo = shippingInfo;
    }

    // 배송지 변경
    public void changeShippingInfo(ShippingInfo shippingInfo) {
        if (orderState == OrderState.SHIPPED
                || orderState == OrderState.DELIVERING
                || orderState == OrderState.DELIVERY_COMPLETED) {
            throw new IllegalStateException("출고 이후에는 배송지를 변경할 수 없습니다.");
        }
        this.shippingInfo = shippingInfo;
    }

    // 주문 취소
    public void cancel() {
        if (orderState == OrderState.SHIPPED
                || orderState == OrderState.DELIVERING
                || orderState == OrderState.DELIVERY_COMPLETED) {
            throw new IllegalStateException("출고 이후에는 주문을 취소할 수 없습니다.");
        }
        this.orderState = OrderState.CANCELED;
    }

    // 상품 준비
    public void prepare() {
        if (orderState != OrderState.PAYMENT_WAITING) {
            throw new IllegalStateException("결제 대기 상태에서는 상품을 준비할 수 없습니다.");
        }
        this.orderState = OrderState.PREPARING;
    }
}
```

**결론**

이처럼 요구사항을 분석하면서 주문과 관련된 도메인 모델을 점진적으로 도출해 나갑니다.

처음부터 모든 모델의 상세 구현을 결정하는 것이 아니라, 필요한 모델은 구체적으로 구현하고, 아직 세부 내용이 정해지지 않은 모델은 이름과 역할 정도만 정의할 수 있습니다.

즉, 요구사항을 하나씩 분석하면서 `핵심 개념 → 상태와 규칙 → 기능과 관계`를 단계적으로 모델에 반영해 나가는 방식입니다.

### 엔티티와 밸류

---

도출한 모델은 크게 엔티티와 밸류로 구분할 수 있습니다.

!image.png

**엔티티**

엔티티는 고유한 식별자를 가지며, 식별자를 통해 각 엔티티를 구분할 수 있습니다.

예를 들어, 주문 엔티티에서는 `주문번호`가 식별자가 됩니다. 따라서 두 엔티티의 식별자가 같다면 동일한 엔티티로 판단할 수 있으며, 이를 객체에서는 `equals()`와 `hashCode()`를 통해 구현할 수 있습니다.

엔티티의 식별자를 생성하는 시점과 방식은 도메인의 특성과 사용하는 기술에 따라 달라지며, 일반적으로 다음과 같은 방법을 사용합니다.

- 특정 규칙에 따라 생성
- UUID, Nano ID와 같은 고유 식별자 생성기를 사용
- 사용자가 직접 입력
- 일련번호를 사용

온라인 쇼핑몰의 주문번호처럼 시간이나 특정 값을 조합해 식별자를 생성할 수도 있습니다. 어떤 방식을 사용하든 식별자가 중복되지 않는 것이 중요합니다.

일련번호 방식은 보통 DB의 자동 증가 기능을 사용합니다. Oracle은 시퀀스, MySQL은 AUTO_INCREMENT를 사용합니다.

자동 증가 방식은 DB에 저장한 후에 식별자가 생성되므로 INSERT 이전에는 식별자를 알 수 없다는 차이가 있습니다. 반면 UUID 등은 엔티티 생성 전에 식별자를 미리 만들 수 있습니다.

**밸류 타입**

`ShippingInfo` 클래스를 예로 들면 `receiverName`과 `receiverPhoneNum`은 받는 사람이라는 하나의 개념을 나타내고, `shippingAdd1`, `shippingAdd2`, `shippingZipCode`는 주소라는 하나의 개념을 나타냅니다.

밸류 타입은 이처럼 개념적으로 하나의 의미를 표현할 때 사용할 수 있습니다.

`Receiver`는 받는 사람이라는 도메인 개념을 표현합니다. 주소 역시 `Address`라는 밸류 타입으로 표현할 수 있습니다.

```kotlin
public class Receiver {
    private String name;
    private String phoneNum;
}
```

```kotlin
public class Address {
    private String address1;
    private String address2;
    private String zipCode;
}
```

따라서 `ShippingInfo`를 아래처럼 구성하면 각 데이터가 어떤 의미를 가지는지 쉽게 파악할 수 있습니다.

```kotlin
public class ShippingInfo {
    private Receiver receiver;
    private Address address;
}
```

밸류 타입이 반드시 여러 개의 데이터를 가져야 하는 것은 아닙니다. 하나의 값을 가지더라도 그 값의 의미를 명확하게 표현하기 위해 사용할 수 있습니다.

예를 들어 `OrderLine`의 `price`와 `amounts`는 모두 돈을 의미하므로 `Money`라는 밸류 타입으로 표현할 수 있습니다.

이렇게 하면 단순히 `int`를 사용하는 것보다 해당 값이 금액을 의미한다는 것을 코드만 보고도 알 수 있고, 돈과 관련된 계산 기능도 `Money`에 추가할 수 있습니다.

```kotlin
public class Money {
    private int value;

    public Money add(Money money) {
        return new Money(this.value + money.value);
    }

    public Money multiply(int multiplier) {
        return new Money(this.value * multiplier);
    }
}
```

```kotlin
public class OrderLine {
    private Money price;
    private Money amounts;
}
```

[ 밸류 타입의 불변성 ]

밸류 객체의 데이터를 변경할 때는 기존 객체의 값을 직접 변경하기보다 변경된 값을 가지는 새로운 밸류 객체를 생성하는 방식을 선호하며, 이를 위해 밸류 객체를 불변 으로 만드는 것이 좋습니다.

예를 들어 `Money`가 변경 가능하다면 다음과 같은 문제가 발생할 수 있습니다.

```kotlin
Money price = ...;
OrderLine orderLine = new OrderLine(product, price, quantity);

price.setValue(x);
```

`OrderLine`이 사용하고 있는 `price`까지 변경되어 의도하지 않은 값이 반영될 수 있습니다.

따라서 `Money`에 setter를 제공하지 않고 값을 변경할 때마다 새로운 `Money` 객체를 생성하도록 만드는 것이 안전합니다.

[ 밸류 타입의 동등성 ]

밸류 타입은 엔티티처럼 식별자로 구분하지 않고, 모든 속성의 값이 같으면 같은 객체로 판단하기에 `equals()`와 `hashCode()`도 모든 속성 값을 기준으로 구현해야합니다.

**엔티티 식별자와 밸류 타입**

엔티티의 식별자는 `String`으로 표현하는 경우가 많지만, 식별자 역시 단순한 문자열이 아니라 도메인에서 특별한 의미를 가지는 값인 경우가 많기에, 식별자도 밸류 타입으로 표현하면 그 의미를 코드에 명확하게 드러낼 수 있습니다.

예를 들어 `Order`의 식별자를 단순한 `String` 대신 `OrderNo`라는 밸류 타입으로 표현할 수 있습니다.

```kotlin
public class OrderNo {
    private String value;
}

public class Order {
    private OrderNo orderNo;
}
```

**도메인 모델에 Setter 사용 지양**

도메인 모델에서 `set` 메서드는 도메인의 핵심 개념이나 의도를 코드에서 사라지게 할 수 있기 때문에 사용을 지양합니다.

예를 들어 배송지를 변경하는 행위라면 다음과 같이 표현할 수 있습니다.

```kotlin
public void changeShippingInfo(ShippingInfo shippingInfo) {
    this.shippingInfo = shippingInfo;
}
```

반면 다음과 같이 `set`을 사용하면 단순히 값을 변경하는 것처럼 보입니다.

```kotlin
public void setShippingInfo(ShippingInfo shippingInfo) {
    this.shippingInfo = shippingInfo;
}
```

`completePayment()`처럼 결제 완료라는 의미가 드러나는 메서드와 달리, `setOrderState()`는 단순히 상태값만 변경하는 것인지, 상태 변경에 따른 추가 로직이 필요한 것인지 코드만으로 알기 어렵습니다.

즉, `set` 메서드를 사용하면 상태 변경과 관련된 도메인 지식이 코드에서 드러나지 않을 수 있습니다.

또한 Setter를 사용하면 객체를 생성한 뒤 필요한 값을 하나씩 설정하면서 불완전한 상태의 객체가 만들어질 수 있습니다**.**

```kotlin
Order order = new Order();

order.setCustomer(customer);
order.setShippingInfo(shippingInfo);
order.setState(OrderState.PREPARING);
```

이 과정에서 `customer` 설정을 누락하더라도 객체가 생성될 수 있습니다.

따라서 필요한 데이터를 생성자의 인자로 전달하여 객체가 생성되는 시점에 항상 올바른 상태를 갖도록 하는 것이 좋습니다.

```kotlin
public Order(Customer customer, ShippingInfo shippingInfo) {
    // 필요한 값 검증
    this.customer = customer;
    this.shippingInfo = shippingInfo;
}
```

하지만, `private set`은 조금 다릅니다. 외부에서는 값을 변경할 수 없고 도메인 객체 내부에서 필요한 경우에만 상태를 변경하기 위한 용도로 사용할 수 있습니다.

중요한 것은 단순히 Setter를 금지하는 것이 아니라, 도메인의 의미와 의도가 메서드 이름을 통해 드러나도록 설계하는 것입니다.

### 도메인 용어와 유비쿼터스 언어

---

코드를 작성할 때는 도메인에서 사용하는 용어를 코드에 그대로 반영하는 것이 중요합니다. 코드가 도메인의 의미를 표현하지 못하면 개발자는 코드만 보고 그 의미와 규칙을 별도로 해석해야 하기 때문입니다.

예를 들어 배송 상태를 단순히 `STEP1`, `STEP2`와 같이 표현하면 `verifyStep1OrStep2()`라는 메서드가 어떤 규칙을 의미하는지 코드만으로 파악하기 어렵습니다. 

반면 `PAYMENT_WAITING`, `PREPARING`처럼 업무의 의미를 나타내는 이름을 사용하면 `verifyPaymentWaitingOrPreparing()`처럼 코드 자체에서 어떤 상태를 검증하는지 알 수 있습니다.

즉, 도메인 용어를 코드에 반영하면 코드의 의미를 해석하는 과정이 줄어들고, 잘못된 의미 전달로 인한 버그도 줄일 수 있습니다.

**유비쿼터스 언어**

도메인 전문가, 관계자, 개발자가 도메인과 관련된 공통의 언어를 정의하고, 이를 대화/문서/도메인 모델/코드 등에서 일관되게 사용하는 것을 의미합니다.

이를 통해 같은 개념을 서로 다른 용어로 표현하면서 발생하는 모호함을 줄이고, 비즈니스 요구사항과 코드 사이의 의미적 차이를 줄일 수 있습니다.
