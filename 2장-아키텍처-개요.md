### 네 개의 영역

---

**표현 영역**

!image.png

사용자의 요청을 받아 응용 영역에 전달하고, 처리된 결과를 다시 사용자에게 전달하는 역할을 합니다.

Spring MVC는 표현 영역을 구현하기 위한 기술에 해당합니다. 표현 영역에서의 사용자는 웹 브라우저를 사용하는 사람일 수도 있고, 다른 외부 시스템일 수도 있습니다.

표현 영역은 HTTP 요청을 응용 영역이 필요로 하는 형식으로 변환하여 전달하고, 응용 영역에서 처리한 결과를 HTTP 응답 형식으로 변환하여 사용자에게 전달합니다.

**응용 영역**

응용 영역은 시스템이 사용자에게 제공해야 하는 기능을 구현하고, 이를 위해 도메인 영역의 도메인 모델을 사용합니다. 응용 서비스는 비즈니스 로직을 직접 수행하기보다는 도메인 모델에 로직 수행을 위임하는 역할을 합니다.

```kotlin
class CancelOrderService {
    @Transactional
    fun cancelOrder(orderId: String) {
        val order = findOrderById(orderId)
        if (order == null) throw Exception()
        order.cancel()
    }
}
```

**도메인 영역**

도메인의 핵심 비즈니스 로직을 구현합니다. 예를 들어 주문 도메인이라면 배송지 변경, 결제 완료, 주문 취소와 같은 도메인 규칙을 도메인 모델에 구현합니다.

**인프라스트럭처 영역**

!image.png

시스템의 구현 기술을 다룹니다. RDBMS 연동, Redis 연동, 메시지 큐를 통한 메시지 송수신 등 실제 기술과의 연동을 담당합니다.

즉, 인프라스트럭처 영역은 비즈니스의 논리적인 개념보다는 이를 구현하기 위한 기술적인 부분을 다룹니다.

### 계층 구조 아키텍처

---

!image.png

표현, 응용, 도메인 영역은 각각 역할이 명확하게 나뉘어 있어 계층 구조를 적용하기에 적합합니다.

계층 구조에서는 기본적으로 상위 계층이 하위 계층에 의존하며, 반대 방향의 의존은 허용하지 않습니다. 엄격한 계층 구조에서는 바로 아래 계층만 의존하지만, 필요에 따라 하위 계층을 건너뛰어 의존하기도 합니다.

하지만 이 구조를 그대로 적용하면 도메인이나 응용 영역이 인프라스트럭처의 구체적인 기술에 의존하는 문제가 발생할 수 있습니다.

예를 들어 도메인의 가격 계산 로직에서 할인 금액을 계산하기 위해 Drools 같은 특정 룰 엔진을 직접 사용한다면, 도메인 로직이 특정 인프라 기술에 종속됩니다. 이렇게 되면 룰 엔진을 다른 기술로 변경하기 어렵고, 도메인 로직을 테스트하기도 어려워집니다.

따라서 도메인 영역이 특정 구현 기술에 직접 의존하지 않도록 추상화하고, 인프라스트럭처가 그 추상화를 구현하도록 하는 방식이 필요합니다. 이것이 이후에 다룰 의존 역전 원칙(DIP)과 연결됩니다.

### DIP

---

!image.png

가격 할인 계산을 예로 들면, `CalculateDiscountService`는 고수준 모듈로, 가격 할인 계산이라는 의미 있는 하나의 기능을 제공합니다.

이를 위해 고객 정보 조회, 할인 규칙 실행 등의 하위 기능이 필요하고, JPA나 Drools와 같은 저수준 모듈이 이를 구현합니다.

문제는 고수준 모듈이 저수준 모듈에 직접 의존하면 구현 변경이 어렵고 테스트하기도 어려워진다는 것입니다.

DIP는 이 문제를 해결하기 위해 저수준 모듈이 고수준 모듈에 의존하도록 의존성을 역전시키며, 이를 인터페이스로 구현합니다.

`CalculateDiscountService` 입장에서는 룰 적용을 Drools로 구현했는지 Java로 구현했는지는 중요하지 않습니다. 단지 고객 정보와 구매 정보에 룰을 적용해 할인 금액을 계산한다는 것만 중요합니다.

⇒ `CalculateDiscountService`에서 Drools에 직접 의존하는 코드를 제거하고, `RuleDiscounter` 인터페이스를 생성자로 주입받도록 합니다.

또한, 룰 적용 클래스는 `RuleDiscounter`를 구현하여 사용합니다. 이때 `CalculateDiscountService`는 Drools의 구체적인 구현을 알 필요 없이, `RuleDiscounter`를 통해 룰을 적용하면 됩니다.

!image.png

`CalculateDiscountService`는 구현 기술에 직접 의존하지 않고 추상화한 인터페이스만 의존할 뿐입니다.

이때, 인터페이스는 고수준 모듈, 구현체는 저수준 모듈에 속하며, 저수준 모듈이 고수준 모듈에 의존합니다.

( 고수준 모듈이 필요로 하는 추상화를 정의하며 저수준 모듈이 그 추상화를 구현하도록 의존 방향을 역전하기 때문 )

의존관계역전 법칙을 사용하면 구현 기술이 바뀌더라도 `CalculateDiscountService`를 수정할 필요가 없고 단지 저수준 구현 객체를 생성하는 코드만 변경하면 됩니다.

**DIP 주의사항**

```kotlin
도메인(정책)
   ↓
도메인이 정의한 추상화
   ↑
Drools(기술)
```

DIP는 단순히 인터페이스를 추가하는 것이 아니라, 고수준 모듈이 저수준 모듈의 인터페이스에 의존하는 구조를 뒤집어, 고수준 모듈이 자신의 필요에 맞는 추상화를 정의하고 저수준 모듈이 이를 구현하도록 만드는 것입니다.

→ 고수준의 정책을 기준으로 추상화를 만들고, 저수준의 구현이 그 추상화에 의존하게 만드는 것

**DIP와 아키텍처**

!image.png

인스라스트럭처 영역은 구현 기술을 다루는 저수준 모듈이고, 응용/도메인 영역은 고수준 모듈입니다.

기존의 계층 구조와 달리 DIP를 적용한 구조는 위와 같습니다.

→ 인스라스트럭처에 위치한 클래스가 도메인이나 응용 영역에 정의한 인터페이스를 상속받아 구현하는 구조가 되기 때문

[ DIP를 적용한 구조 ]

DIP를 적용하면 응용 영역과 도메인 영역에 영향을 최소화하며, 구현체를 변경하거나 추가 가능

!image.png

### 도메인 영역의 주요 구성요소

---

도메인 영역의 모델은 도메인의 주요 개념을 표현하며 핵심 로직을 구현합니다. 

**도메인 영역의 구성 요소**

- 엔티티
- 밸류
- 애그리거트 : 연관된 엔티티와 밸류 객체를 하나로 묶은 것
- 리포지터리 : 도메인 모델의 영속성을 처리
- 도메인 서비스 : 특정 엔티티에 속하지 않은 도메인 로직을 제공

**엔티티 & 밸류**

도메인 모델의 엔티티는 식별자를 기준으로 동일성을 판단하며, 데이터와 데이터를 변경하는 기능을 함께 캡슐화합니다. 이를 통해 객체의 상태를 보호하고 도메인 규칙을 객체 내부에서 관리할 수 있습니다.

반면 밸류 타입은 여러 데이터를 하나의 개념으로 묶어 표현하며, 별도의 식별자 없이 값 자체로 동일성을 판단합니다. 예를 들어 도시, 도로명, 우편번호를 `Address`라는 하나의 밸류 타입으로 표현할 수 있습니다.

하지만 RDBMS는 객체의 중첩 구조를 그대로 저장하기 어렵기 때문에, 밸류 타입을 여러 컬럼으로 펼치거나 별도의 테이블로 분리해야 합니다.

즉, 객체에서는 하나의 개념으로 표현할 수 있는 밸류 타입을 관계형 데이터베이스에서는 여러 컬럼이나 테이블로 나누어 저장해야 하며, 이러한 객체 모델과 관계형 데이터 모델의 차이를 매핑하는 것이 ORM의 주요 역할입니다.

**애그리거트**

도메인이 커질수록 엔티티와 밸류가 많아지면서 모델의 관계가 복잡해집니다. 이때 개별 객체에만 집중하면 전체 모델의 관계와 구조를 파악하기 어려워집니다.

애그리거트는 관련된 엔티티와 밸류를 하나의 군집으로 묶어 상위 개념으로 표현하는 방법입니다.

예를 들어 `주문`은 주문 정보, 주문자, 배송지, 주문 상품 등 여러 객체로 구성되지만, 이를 하나의 주문 애그리거트로 묶어 관리할 수 있습니다. 즉, 애그리거트는 복잡한 도메인 모델을 상위 수준에서 이해하고 관리하기 위한 단위입니다.

애그리거트를 사용하면 개별 객체가 아닌 관련 객체를 하나의 군집으로 묶어 모델을 바라볼 수 있어, 복잡한 도메인 모델을 상위 수준에서 관리할 수 있습니다.

애그리거트는 군집에 속한 객체를 관리하는 `루트 엔티티`를 가집니다. 외부에서는 루트가 제공하는 기능을 통해서만 애그리거트 내부의 객체에 접근하며, 이를 통해 내부 구현을 숨기고 애그리거트 단위로 캡슐화할 수 있습니다.

**리포지터리**

도메인 객체를 지속적으로 사용하려면 RDBMS, NoSQL, 파일 등 물리적인 저장소에 객체를 보관해야 합니다. 리포지터리는 이러한 영속화에 필요한 기능을 추상화한 도메인 모델입니다.

리포지터리는 일반적으로 애그리거트 단위로 저장하고 조회합니다. 루트 엔티티가 애그리거트 내부 객체를 관리하기 때문에 외부에서는 루트를 통해 애그리거트 전체를 다룹니다.

도메인 관점에서 리포지터리는 영속화에 필요한 기능을 추상화한 고수준 모듈이며, 실제 DB나 저장소를 이용해 이를 구현하는 클래스는 인프라스트럭처 영역에 해당합니다.

응용 서비스는 의존 주입과 같은 방식을 사용해서 실제 리포지터리 구현 객체에 접근하며, 응용 서비스는 리포지터리와 밀접한 연관이 있습니다.

- 응용 서비스는 필요한 도메인 객체를 구하거나 저장할 때, 리포지터리를 사용
- 응용 서비스는 트랜잭션을 관리하는데, 트랜잭션 처리는 리포지터리 구현 기술의 영향을 받음

### 요청 처리 흐름

---

표현 영역은 사용자가 전송한 데이터의 형식을 검증하고, 응용 서비스가 요구하는 형태로 변환하여 전달합니다. 이후 실제 기능의 실행은 응용 서비스에 위임합니다.

응용 서비스는 도메인 모델을 이용해 애플리케이션의 기능을 구현합니다. 기능에 필요한 도메인 객체를 리포지터리에서 조회하거나 새로 생성하고, 필요한 경우 여러 도메인 객체를 조합하여 기능을 수행한 뒤 변경된 객체를 저장합니다.

예매나 취소처럼 도메인의 상태를 변경하는 기능은 트랜잭션으로 관리하여 작업의 원자성과 일관성을 보장해야 합니다.

### **인프라스트럭처 개요**

---

인프라스트럭처 영역은 표현·응용·도메인 영역을 지원하며, 영속성, 트랜잭션, SMTP 등 외부 기술과 프레임워크를 이용한 구현을 담당합니다.

DIP를 적용하면 도메인과 응용 영역에서 필요한 기능을 인터페이스로 정의하고, 인프라스트럭처 영역이 이를 구현하도록 구성할 수 있습니다. 이를 통해 인프라 기술에 대한 결합도를 낮추고 유연성과 테스트 용이성을 높일 수 있습니다.

다만 인프라스트럭처에 대한 의존을 무조건 제거할 필요는 없습니다. 예를 들어 도메인 모델에 Spring의 `@Transactional`이나 JPA의 `@Entity` 같은 어노테이션을 사용하는 것이 별도의 복잡한 설정을 만드는 것보다 실용적일 수 있습니다. DIP의 장점을 얻는 것과 구현의 편의성을 적절히 균형 잡는 것이 중요합니다.

또한 표현 영역은 사용하는 프레임워크에 맞게 구현됩니다. 예를 들어 Spring MVC를 사용하면 Spring MVC의 방식에 맞게, Vert.x를 사용하면 Vert.x의 방식에 맞게 표현 영역을 구성합니다.

### 모듈 구성

---

!image.png

아키텍처의 각 영역은 별도 패키지로 구성합니다. 패키지 구성에 정답은 없지만, 일반적으로 아래와 같은 기준을 적용합니다.

- 도메인이 크다면 하위 도메인별로 패키지를 분리합니다.
- 도메인 모듈은 애그리거트를 기준으로 다시 패키지를 구성합니다.
- 하나의 애그리거트에 속한 엔티티, 밸류, 리포지터리 등은 같은 패키지에 위치시킵니다.
- 패키지를 지나치게 세분화할 필요는 없으며, 한 패키지에 코드가 과도하게 몰리지 않는 수준으로 관리합니다. 일반적으로 10~15개 정도의 패키지를 기준으로 생각할 수 있습니다.
